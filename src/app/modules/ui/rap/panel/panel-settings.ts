export interface PanelSettings {
  footer: boolean;
  left: boolean;
  right: boolean;
  top: boolean;
  form?: boolean;
}

export interface PanelButtonSettings {
  rightToggle: boolean;
  plus: boolean;
  cancel: boolean;
  save: boolean;
}